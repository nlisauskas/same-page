import React from 'react';
const Project = ({project}) =>
    <div className="single-payment" key={project.id}>
      <p>This is a project between a landlord and a contractor.</p>
    </div>
export default Project;
