import React from 'react';
const Project = ({project}) =>
    <div className="single-payment" key={project.id}>
      <p>This is a project between a landlord and a contractor. The tenant is looped in to stay up to date on project progress and they are able to directly request work be done that the landlord can either approve via text or the work can just be performed if under a certain $$ threshold.</p>
    </div>
export default Project;
